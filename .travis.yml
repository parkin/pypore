language: python
python:
  - "2.7"

before_install:
  - uname -a
  - free -m
  - df -h
  - python -V
  - sudo apt-get update -qq
  - pip install .

script:
  - nosetests

notifications:
  email: false

